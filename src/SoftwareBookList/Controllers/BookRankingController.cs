using Microsoft.AspNetCore.Mvc;
using SoftwareBookList.Models;

namespace SoftwareBookList.Controllers
{
    public class BookRankingController : Controller
    {
    //    public IActionResult RatingTable()
    //    {
    //        // Retrieve data from your data source
    //        var ratingData = GetRatingData(); // Implement this method to get your data

    //        // Create a list of RatingItem objects
    //        List<RatingItem> ratingList = new List<RatingItem>();
    //        int rank = 1;
    //        foreach (var item in ratingData)
    //        {
    //            ratingList.Add(new RatingItem { Rank = rank, Name = item.Name, Score = item.Score });
    //            rank++;
    //        }

    //        return View(ratingList);
    //    }
    }
}
